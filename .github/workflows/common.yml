name: Reusable Workflow
on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello ${{ inputs.name }}!"